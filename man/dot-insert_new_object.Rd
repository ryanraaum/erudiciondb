% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{.insert_new_object}
\alias{.insert_new_object}
\title{Create and insert a new object in one operation}
\usage{
.insert_new_object(
  connection,
  object_type,
  object,
  validate_function = NULL,
  augment_function = NULL,
  stage = 0
)
}
\arguments{
\item{connection}{Database connection or pool}

\item{object_type}{Type of object (singular form: "item", "person", etc.)}

\item{object}{List containing object data (partial or complete)}

\item{validate_function}{Optional validation function to check object integrity}

\item{augment_function}{Optional augmentation function to enrich object data}

\item{stage}{Activation stage (0 = active, -1 = inactive, default: 0)}
}
\value{
UUID of the inserted object
}
\description{
Convenience wrapper that combines \code{.new_object()} and \code{.insert_one()} into a
single operation for common object creation workflow.
}
\details{
Equivalent to calling:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{new_obj <- .new_object(connection, object_type, object, validate_function, augment_function, stage)
.insert_one(connection, new_obj)
}\if{html}{\out{</div>}}

This is the standard way to create and insert simple objects. For items with
creators (which require personlists and item_persons), use the full
\code{ErudicionDB$insert_new_object()} method instead.
}
\keyword{internal}
