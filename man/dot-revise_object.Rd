% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{.revise_object}
\alias{.revise_object}
\title{Create new revision of an object with updated fields}
\usage{
.revise_object(connection, this_object, ...)
}
\arguments{
\item{connection}{Database connection or pool}

\item{this_object}{The current object (must have object_type and object_id fields)}

\item{...}{Named parameters with field updates (e.g., title = "New Title")}
}
\value{
List containing the revised object with:
\itemize{
\item Specified fields updated to new values
\item revision incremented to next available number
\item All other fields unchanged from original
}
}
\description{
Takes an existing object and specified field updates, creates a new revision
with the changes. Does NOT insert to database - only prepares the revised object.
}
\details{
Only updates fields that exist in the table schema and are provided in \code{...}.
The object_id field cannot be updated. Uses \code{.update_it()} helper to safely
replace values (preserving existing if new is NULL). The new revision number
is calculated via \code{.next_revision()}.
}
\note{
This function only prepares the revised object - it does NOT insert it to the
database or destage the old revision. Use \code{.update_object()} for the complete
update workflow with transaction safety.
}
\keyword{internal}
