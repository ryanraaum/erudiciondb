% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{.find_item_by_year_volume_page}
\alias{.find_item_by_year_volume_page}
\title{Find items by citation metadata (year, volume, first page)}
\usage{
.find_item_by_year_volume_page(connection, year, volume, first_page)
}
\arguments{
\item{connection}{Database connection or pool}

\item{year}{Publication year (integer)}

\item{volume}{Journal volume (string)}

\item{first_page}{First page number (string)}
}
\value{
Data frame with columns: item_id, stage, revision, similarity (1), found_by ("year_volume_page")
}
\description{
Searches for items matching all three citation components: publication year,
journal volume, and first page number.
}
\details{
All three parameters are required - returns empty result if any are missing.
Extracts year from the issued date field using database-specific functions:
\itemize{
\item \strong{SQLite}: Uses \code{strftime('\%Y', issued)} for date extraction, LIKE for matching
\item \strong{DuckDB}: Uses \code{year(issued)} function, ILIKE for matching
}

All three criteria must match (AND'd together) for an item to be returned.
}
\note{
This is the second search strategy in \code{.find()} for items (after identifier
search fails). Particularly useful for finding items from older publications
that lack DOIs.
}
\keyword{internal}
