% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{.find_person_identifier}
\alias{.find_person_identifier}
\title{Find person identifiers using flexible search strategies}
\usage{
.find_person_identifier(connection, object_data)
}
\arguments{
\item{connection}{Database connection or pool}

\item{object_data}{List containing search criteria (id_value, id_type, and/or person_id)}
}
\value{
Data frame with person_identifier records plus similarity (1) and found_by columns
}
\description{
Searches person_identifiers table using one of three strategies based on which
fields are provided in the search object.
}
\details{
Uses three prioritized search strategies (stops at first match):
\enumerate{
\item \strong{By id_value}: If id_value provided, match on uppercased id_value_uppercase
(found_by = "identifier"). Most specific search.
\item \strong{By id_type + person_id}: If both provided, match on exact combination
(found_by = "person_and_type"). Useful for checking if a specific person
has a specific type of identifier.
\item \strong{By person_id only}: If only person_id provided, return all identifiers
for that person (found_by = "person_id").
}

All matches return similarity=1 (exact match).
}
\note{
The id_value_uppercase field stores normalized (uppercased) identifier values
for case-insensitive matching. Used by \code{.find()} when searching for person_identifier
objects.
}
\keyword{internal}
