% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augmentors.R
\name{.augment_person}
\alias{.augment_person}
\title{Augment person object with computed fields}
\usage{
.augment_person(new_p)
}
\arguments{
\item{new_p}{Person object (list with person fields)}
}
\value{
Person object with computed fields filled in
}
\description{
Enriches person data with ASCII name variants, referents for display, and
sorting referent for alphabetization. Most complex augmentor function.
}
\details{
Computes fields if not already provided:

\strong{ASCII variants} (for fuzzy matching):
\itemize{
\item \code{ascii_given_names}: Lowercase ASCII transliteration of combined primary + other given names
\item \code{ascii_surnames}: Lowercase ASCII transliteration of surnames (excludes particles)
}

\strong{Referents} (for display):
\itemize{
\item \code{short_referent}: Primary given names only (e.g., "Jean-Paul")
\item \code{long_referent}: Full name with all components in order:
prefix, primary given names, initials of other given names, particles, surnames, suffix
(e.g., "Dr. Jean-Paul M. de Martínez, Jr.")
\item \code{sorting_referent}: Lowercase ASCII surname with non-dropping particle
(e.g., "de martinez" for "Martínez")
}

\strong{Flags}:
\itemize{
\item \code{also_known_as}: Defaults to FALSE
\item \code{comma_suffix}: Defaults to FALSE (controls comma before suffix in long_referent)
}
}
\note{
Sorting referent handles mononyms (people with only given names) by using
primary given name as sort key. Non-dropping particles are included in sort
(e.g., "de Gaulle" sorts under "D"), but dropping particles are not.
}
\keyword{internal}
