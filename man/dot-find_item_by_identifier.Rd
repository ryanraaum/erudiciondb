% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interface.R
\name{.find_item_by_identifier}
\alias{.find_item_by_identifier}
\title{Find items by external identifiers (DOI, PMID, PMCID)}
\usage{
.find_item_by_identifier(connection, doi = NULL, pmid = NULL, pmcid = NULL)
}
\arguments{
\item{connection}{Database connection or pool}

\item{doi}{Digital Object Identifier (optional)}

\item{pmid}{PubMed ID (optional)}

\item{pmcid}{PubMed Central ID (optional)}
}
\value{
Data frame with columns: item_id, stage, revision, similarity (1), found_by ("identifier")
}
\description{
Searches for items matching any of the provided external identifiers using
case-insensitive LIKE matching.
}
\details{
Builds a SQL query with OR'd WHERE clauses for each provided identifier:
\itemize{
\item \strong{SQLite}: Uses LIKE for case-insensitive matching
\item \strong{DuckDB}: Uses ILIKE for case-insensitive matching
}

All provided identifiers are OR'd together - any match succeeds. Returns all
matching items with similarity=1 (exact match) and found_by="identifier".
}
\note{
Returns empty tibble if no identifiers provided or no matches found. This is
the first (highest priority) search strategy in \code{.find()} for items.
}
\keyword{internal}
